import { Title, Layout } from '../../elements';

function EventScreen({ navigation, route }) {
  return (
    <Layout scrollable>
      <Title>Event Screen</Title>
    </Layout>
  );
}

export default EventScreen;
